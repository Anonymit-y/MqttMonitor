# mosquitto.conf

# Listen on all network interfaces (port 1883)
listener 1883 0.0.0.0

# Allow farm sensors to connect without a username/password
allow_anonymous true

# Persist data to the hard drive so messages aren't lost on restart
persistence true
persistence_location /mosquitto/data/

# Log output to the standard docker logs
log_dest stdout
